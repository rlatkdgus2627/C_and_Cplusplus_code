#include <bits/stdc++.h>

using namespace std;

int T, q, x;

int main(){
    cin.tie(0);
    ios::sync_with_stdio(0);

    cin >> T;
    while(T--){
        cin >> q >> x;
        if(q == 1)  
    }
    return 0;
}